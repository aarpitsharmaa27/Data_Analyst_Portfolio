import numpy as np

a = np.array([235,62,45,424,44,40])

print(np.sum(a))
print(np.min(a))
print(np.max(a))
print(np.size(a))
print(np.mean(a))
print(np.cumsum(a))  # adding values one by one
print(np.cumprod(a))    # Multiplying values one by one




